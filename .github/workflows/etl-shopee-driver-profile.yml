name: ETL Shopee - Perfil do Motorista (Diário)

on:
  schedule:
    # Roda todo dia às 06h00 BRT (= 09h UTC)
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  etl-shopee-driver-profile:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: ''

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir -r requirements.txt

      - name: Instalar browsers do Playwright
        run: |
          sudo apt-get install -y libasound2t64 2>/dev/null || true
          playwright install chromium --with-deps

      - name: Rodar Pipeline ETL
        env:
          NEON_DATABASE_URL: ${{ secrets.NEON_DATABASE_URL }}
          SHOPEE_EMAIL: ${{ secrets.SHOPEE_EMAIL }}
          SHOPEE_PWD: ${{ secrets.SHOPEE_PWD }}
        run: |
          python -m src.pipelines.shopee_driver_profile_pipeline

      - name: Upload de artifacts (em caso de erro)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: erro-driver-profile
          path: data/raw/
          retention-days: 7
